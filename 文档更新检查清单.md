# 文档更新检查清单

## 一、系统架构文档.md 需要更新的内容

### 1. 新增功能模块
- [x] **target_version 自动获取功能**
  - 新增函数：`resolve_version_target()`, `get_target_version()`, `get_highest_version()`
  - 新增函数：`parse_version()`, `compare_versions()`
  - 支持版本号自动替换和最高版本自动获取

### 2. 版本管理机制更新
- [x] **版本号比较逻辑**
  - 支持复杂版本号格式：1.0.6.2-4, 1.0.6.0-4-patch-1
  - 支持 alpha, beta, rc, patch 标识符
  - 自动排序获取最高版本

- [x] **模式到分类映射优化**
  - 新增 `get_category_by_mode()` 函数
  - 完整映射关系表
  - None 值异常处理

### 3. 配置参数新增
- [x] **target_version 配置项**
  - 配置格式：`{sheet_name}_target_version_{分类名称}`
  - 支持自动获取最高版本

- [x] **projects 配置项**
  - 配置格式：`{sheet_name}_projects_{分类名称}`
  - 支持多项目换行分隔

### 4. 日志系统（已更新）
- [x] DynamicFileHandler 生命周期
- [x] 用例分隔符样式
- [x] 阶段分隔符样式
- [x] 模块命名规范

## 二、逻辑流程文档.md 需要更新的内容

### 1. 主流程更新
- [x] 添加 target_version 自动获取流程
- [x] 添加版本号比较流程
- [x] 添加模式映射流程

### 2. 新增流程图
- [x] **target_version 自动获取流程**
  - 配置读取
  - RDM 平台刷新
  - 最高版本获取

- [x] **版本号比较流程**
  - 版本号解析
  - 标识符权重
  - 排序比较

- [x] **模式映射流程**
  - 模式后缀提取
  - 分类映射
  - 异常处理

### 3. 现有流程优化
- [x] **版本路径获取流程**
  - 添加 target_version 处理逻辑
  - 添加自动获取最高版本逻辑

- [x] **备份恢复流程**
  - 优化流程图展示
  - 添加版本+模式双重匹配逻辑

- [x] **升级流程区分**
  - 区分升级系统接管和本地升级
  - 添加 xsa.json 预修改流程

### 4. 流程图优化
- [x] 横向/纵向调整
- [x] 字体大小优化
- [x] 直观性改进

---

**更新状态总结：**
- ✅ 高优先级代码功能已实现
- ✅ 系统架构文档已更新
- ✅ 逻辑流程文档核心流程已更新
- ✅ 低优先级流程图优化已处理

## 三、需要对比的代码文件

- [x] dpiinstall.py - 主安装逻辑 (business/install.py)
- [x] dpi.py - DPI 操作封装 (device/dpi.py)
- [x] extract_release_path.py - 版本提取 (business/install.py 已整合)
- [x] log_handler.py - 日志处理器 (utils/log_handler.py)
- [x] common.py - 通用工具 (utils/common.py)

## 四、更新优先级

**高优先级（已完成）：**
1. [x] target_version 自动获取功能（核心新功能）- 已实现
2. [x] 版本号比较逻辑（核心新功能）- 已实现
3. [x] 模式映射优化（核心新功能）- 已实现

**中优先级（已完成）：**
4. [x] 版本路径获取流程更新
5. [x] 备份恢复流程优化
6. [x] 升级流程区分

**低优先级（已完成）：**
7. [x] 流程图美观优化
8. [x] 文档格式统一

---

**更新状态：**
- ✅ 代码功能已完成实现
- ✅ 循环导入问题已修复（utils/xml_helper.py）
- ✅ 系统架构文档已更新
- ✅ 逻辑流程文档已更新

## 五、模块化重构导入修复

### 1. 常量文件移动
- [x] `core/constants.py` → `device/dpi_constants.py`
  - 更新所有业务文件的导入路径

### 2. 拼写错误修复
- [x] 修复 `house_ipsegsfifle` → `house_ipsegsfile`
  - 影响文件：log_audit.py, eu_policy.py, bzip.py, mirrorvlan.py, pcapdump.py, log_active.py

### 3. 缺失常量补加
- [x] 添加 `eu_active_resource_rulefile` 到 device/dpi_constants.py
  - 路径：`/opt/dpi/euconf/rule/eu_active_resource.rule`

### 4. 验证结果
- [x] 所有 business 模块导入测试通过
- [x] main.py -h 正常运行
- [x] 所有 Python 文件语法检查通过
